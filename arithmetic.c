#include <stdio.h>

int main(){

    /* constant  = fixed value that cannot be altered by the program during its
    execution */

    // arithmetic operators
    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement

    int x = 5;
    int y = 2;

    //int z = x + y;
    //int z = x -y;
    //int z = x * y;
    //float z = x/(float)y;
    //int z = x % y;
    
    y++;
    
    printf("%d", y);

    return 0;
}